## 使用文本对象选择和操作文本

在 Helix 中，**文本对象（textobjects）** 是一种以结构化方式选择、操作和处理文本的方法。它们允许你基于文本的结构或用途来引用文本块，例如单词、句子、段落，甚至是函数或代码块。

![Textobject demo](https://user-images.githubusercontent.com/23398472/124231131-81a4bb00-db2d-11eb-9d10-8e577ca7b177.gif)
![Textobject tree-sitter demo](https://user-images.githubusercontent.com/23398472/132537398-2a2e0a54-582b-44ab-a77f-eb818942203d.gif)

* `ma` - 选择对象的外围（相当于 Vim 的 `va`，Kakoune 的 `<alt-a>`）
* `mi` - 选择对象内部（相当于 Vim 的 `vi`，Kakoune 的 `<alt-i>`）

| 在 `mi` 或 `ma` 后按的键 | 选择的文本对象                   |
| ------------------ | ------------------------- |
| `w`                | 单词（word）                  |
| `W`                | 大写单词（WORD）                |
| `p`                | 段落（paragraph）             |
| `(`、`[`、`'` 等      | 指定的包围符号对                  |
| `m`                | 最近的包围符号对                  |
| `f`                | 函数（function）              |
| `t`                | 类型或类（Type/Class）          |
| `a`                | 参数或实参（Argument/Parameter） |
| `c`                | 注释（Comment）               |
| `T`                | 测试（Test）                  |
| `g`                | 改动（Change）                |
| `x`                | (X)HTML 元素                |

> 💡 `f`、`t` 等需要当前文档启用 **tree-sitter** 语法解析，并且需要相应的 tree-sitter 查询文件才能正常工作。[目前只有部分语法](./lang-support.md)实现了查询文件，欢迎贡献！

---

## 使用 tree-sitter 文本对象进行导航

通过 **tree-sitter** 和文本对象查询，可以在函数、类、参数和其他代码元素之间进行快速导航。例如：

* 移动到下一个函数：`]f`
* 移动到上一个类型：`[t`
* 以此类推

![Tree-sitter-nav-demo](https://user-images.githubusercontent.com/23398472/152332550-7dfff043-36a2-4aec-b8f2-77c13eb56d6f.gif)

完整参考请查看 [unimpaired](./keymap.html#unimpaired) 小节的键位绑定文档。

> 💡 该功能依赖 **tree-sitter 文本对象**，并且需要对应的查询文件才能正常工作。
